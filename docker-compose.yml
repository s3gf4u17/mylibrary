version: "3.8"
services:
  database:
    build: ./database
    ports:
      - 5432:5432
  backend:
    build: ./backend
    ports:
      - 8000:8000
    depends_on:
      - database
  frontend:
    build: ./frontend
    ports:
      - 80:80